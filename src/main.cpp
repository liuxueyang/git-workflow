#include <bits/stdc++.h>

using namespace std;

int main(int argc, char *argv[])
{
  int i;

  for (i = 0; i < N; ++i) {
    // changes (1) made in branch dev
    cout << i << "\n";
  }

  while (true) {
    // change (2) made on branch hw-rebase-conflict
    // smile
    cout << ":)))" << "\n";
  }

  return 0;
}
