#include <bits/stdc++.h>

using namespace std;

int main(int argc, char *argv[])
{
  for (i = 0; i < N; ++i) {
    // change (1) on branch lxy/hw-squash
    cout << "i = " << i << "\n";
  }

  while (true) {
    // change (2) on branch lxy/hw-squash
    cout << ":))))" << "\n";
  }

  cout << "Goodbye!!!" << "\n";

  return 0;
}
