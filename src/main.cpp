#include <bits/stdc++.h>

using namespace std;

int main(int argc, char *argv[])
{
  for (i = 0; i < N; ++i) {
    // change (2) on branch lxy/edit-commit
    cout << "i = " << i << endl;
  }

  while (true) {
    // change (1) on branch lxy/edit-commit
    cout << ":)))" << "\n";
  }

  return 0;
}
