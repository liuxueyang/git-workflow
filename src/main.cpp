#include <bits/stdc++.h>

using namespace std;

int main(int argc, char *argv[])
{
  int i, N = 2333;

  for (i = 0; i < N; ++i) {
    // change (1) made on branch hw-rebase-conflict
    cout << i * i << "\n";
  }

  while (true) {
    // change (2) made on branch hw-rebase-conflict
    // smile
    cout << ":)))" << "\n";
  }

  return 0;
}
